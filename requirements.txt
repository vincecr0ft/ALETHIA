# Cloud Run runtime deps for the ALETHIA ADK agent.
# CPU-only torch keeps the image small (no CUDA wheels).
--extra-index-url https://download.pytorch.org/whl/cpu

# Agent framework + Gemini
google-adk>=1.32.0
google-genai>=1.9.0

# Phoenix tracing (OpenInference) + OTLP export
openinference-instrumentation-google-adk>=0.1.11
arize-phoenix-otel>=0.9.0
opentelemetry-sdk>=1.27.0
opentelemetry-exporter-otlp-proto-http>=1.27.0

# Phoenix MCP toolset transport
mcp>=1.0.0

# Physics surrogate stack
numpy>=1.26
scipy>=1.11
torch>=2.2.0
scikit-learn>=1.4.0

# Server + config
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.1
